{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // The only package that renders, so the only one that needs the DOM lib.
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx"
  },
  "include": ["src/**/*.ts", "src/**/*.tsx"]
}
